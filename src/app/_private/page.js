import React from 'react'

const PrivateFolder = () => {
  return (
    <div>by putting underscore before folder name we made the folder private and it cannot be get by the routes in url ,by putting %5F in place of underscore in foldername then we get the folder name with putting underscore in url </div>
  )
}

export default PrivateFolder